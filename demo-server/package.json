{
  "name": "demo-server",
  "version": "0.1.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "scripts": {
    "start": "node index.js",
    "build": "npx -p typescript tsc",
    "build:dev": "npx nodemon --exec 'npm run build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4",
    "yaetae": "file:../yaetae"
  },
  "devDependencies": {
    "@types/express": "^4",
    "prettier-plugin-jsdoc": "latest"
  }
}
